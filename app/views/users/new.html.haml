%h1 Rejestracja
= image_tag "registration.png"
= semantic_form_for @user, :html => {:multipart => true} do |f|
  = f.input :login, :label => "Login:", :required => false
  = f.input :email, :label => "Email:", :required => false
  = f.input :password, :label => "Hasło:", :required => false
  = f.input :password_confirmation, :label => "Hasło (ponownie):", :required => false
  = f.submit :value => "Zarejestruj!"